<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">

    <title>Rust + Rocket = Tiny Humans</title>

    <style>
      body {
        background-image: url('paper.jpg');
      }

      .human {
        position: absolute;
      }
    </style>

  </head>
  <body id="TinyHumans">
  </body>
  <script type="text/javascript" src="humans.js"></script>
  <script>
    {% for human in humans %}
      var personSVG = drawPerson();

      var newPerson = document.createElement('svg');
      newPerson.innerHTML = personSVG;
      newPerson.className = 'human';

      newPerson.style.left = "{{human.x}}px";
      newPerson.style.top = "{{human.y}}px";

      document.body.appendChild( newPerson );
    {% endfor %}
  </script>
</html>
